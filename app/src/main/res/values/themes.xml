<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MovieBooking" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="boxStrokeWidth">0dp</item>
        <!-- Customize your theme here. -->
    </style>


    <style name="myButton">
        <item name="android:backgroundTint">@color/semipink</item>
        <item name="boxStrokeWidth">12dp</item>
        <item name="boxBackgroundColor">@color/semipink</item>
        <item name="android:textSize">13dp</item>
        <item name="cornerRadius">9sp</item>
        <item name="android:textColor">@color/white</item>
    </style>


</resources>